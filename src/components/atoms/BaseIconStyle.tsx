import { css } from 'styled-components'

const BaseIconStyle = css`
    width: ${({ theme }) => theme.unit3};
    height: ${({ theme }) => theme.unit3};
    margin-right: ${({ theme }) => theme.unit1};
`

export default BaseIconStyle
